# pipeline output (regenerated every run)
/out/
src.zip

# build output
/target/
dependency-reduced-pom.xml
*.class

# input jars (private / large - place client.jar next to spk_deob.bat, never commit)
*.jar
# ...except the pinned build-time lombok jar used for decompiled sources
!lib/lombok-*.jar

# real name mappings (private knowledge - copy spk_map.example.json to start)
spk_map.json
*map*.json
!spk_map.example.json

# IDE
.idea/
*.iml
.vscode/

# OS
.DS_Store
Thumbs.db

# logs
*.log
